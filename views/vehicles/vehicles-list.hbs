FOUND VEHICLES
<div class='set-of-vehicles'>
  <div class='vehicle-card'>
    <img class='vehicle-img' src="{{this.primaryPhotoUrl}}" alt='' />
    {{#each vehiclesFromApi}}
      <p>
        {{this.make}}
      </p>
    {{/each}}

    {{#if errorMessage}}
      {{errorMessage}}
    {{/if}}
  </div>
</div>